<h1>Blurgit</h1>

<% @articles.each do |article| %>
  <h2>
    <%= link_to article.title, article_path(id: article.id) %>
    <small>Published <%= distance_of_time_in_words_to_now article.publish_date %> ago. </small>
  </h2>

  <%= simple_format truncate(article.body, length: 140) %>

<% end %>
